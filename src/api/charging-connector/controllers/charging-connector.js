'use strict';

/**
 * charging-connector controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::charging-connector.charging-connector');
